firstExamScore = float(input ("Enter the score for the first exam: "))
secondExamScore = float (input ("Enter the score for the second exam: "))
totalScore = (firstExamScore * 0.6) + (secondExamScore * 0.4)
print ("The total score is:", totalScore)
